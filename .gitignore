.env
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.idea/
.vscode/
.DS_Store

# RAG artefacts (rebuilt from ./documents or re-indexed on first run)
faiss_index/
documents/*
!documents/.gitkeep

# ChromaDB runtime data — stored on disk but never committed
# In production use Docker named volumes or set CHROMA_BASE_DIR to a
# persistent path outside the project root.
chroma/


# Legacy JSON config files (data now lives in SQLite)
db_connections.json
users.json
url_sources.json
vanna_trained.json
users.json.bak

# SQLite database (runtime data — not committed)
data/chat.db
chat.db
